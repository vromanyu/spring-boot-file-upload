spring:
  application:
    name: upload-ms
  main:
    banner-mode: off
  threads:
    virtual:
      enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  config:
    import: file:./.spring_env/secrets.yaml
server:
  port: 8082

